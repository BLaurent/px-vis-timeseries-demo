{
  "name": "px-vis-timeseries-demo",
  "version": "0.0.1",
  "description": "Demo to test timeseries component in action",
  "main": "server.js",
  "scripts": {
    "start": "rm -rf bower_components/ && bower cache clean && bower install -p && node server.js"
  },
  "dependencies": {
    "express": "^4.15.2",
    "bower": "^1.8.0"
  },
  "engines": {
    "node": "6.10.x"
  },
  "repository": {
    "url": "https://glitch.com/edit/#!/px-vis-timeseries-demo"
  }
}
